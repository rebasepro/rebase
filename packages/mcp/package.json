{
  "name": "@rebasepro/mcp",
  "version": "0.17.3",
  "description": "Model Context Protocol Server for Rebase \u2014 exposes schema, DB, document, and user management tools to AI assistants.",
  "keywords": [
    "rebase",
    "mcp",
    "model-context-protocol",
    "ai",
    "agents"
  ],
  "homepage": "https://rebase.pro",
  "bugs": {
    "url": "https://github.com/rebasepro/rebase/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/rebasepro/rebase.git",
    "directory": "packages/mcp"
  },
  "license": "MIT",
  "engines": {
    "node": ">=22.22.0"
  },
  "mcpName": "io.github.rebasepro/rebase",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "type": "module",
  "bin": {
    "rebase-mcp": "bin/rebase-mcp.js"
  },
  "scripts": {
    "prepack": "node -e \"const ua=process.env.npm_config_user_agent||'';if(ua.startsWith('pnpm'))process.exit(0);const p=JSON.parse(require('fs').readFileSync('package.json','utf8'));const d=Object.assign({},p.dependencies,p.peerDependencies);const ws=Object.entries(d).filter(([,v])=>String(v).startsWith('workspace:'));if(ws.length){console.error('ERROR: workspace: protocol found \u2014 use pnpm publish, not npm publish. Deps:',JSON.stringify(ws));process.exit(1)}\"",
    "build": "tsc && node ../../tooling/scripts/add-dts-extensions.mjs dist && node ../../tooling/scripts/assert-build-output.mjs",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@rebasepro/client": "workspace:*",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^6.0.3",
    "vitest": "4.1.10"
  },
  "files": [
    "bin/",
    "dist/"
  ],
  "publishConfig": {
    "access": "public"
  }
}
