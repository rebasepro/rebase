// Fixture for the loader's "unsupported extension" diagnostic. `rebase build`
// and a developer's editor both treat this as source; the loader cannot import
// it, and used to skip it without a word. Never executed by any test.
export default null;
