export type ImageData = {
    title: string;
    description: string;
    imageDataUrl?: string; // Should be a data URL e.g., "data:image/png;base64,..."
    imagePath?: string; // Path to the image file
};
