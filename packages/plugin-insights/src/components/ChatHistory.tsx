import React from "react";

export function ChatHistory(props: any) {
    return (
        <div className="p-4 flex items-center justify-center h-full text-gray-500">
            Chat history is currently disabled.
        </div>
    );
}
