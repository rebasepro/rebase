// This file is auto-generated by the Rebase Drizzle generator. Do not edit manually.

import { primaryKey, pgTable, integer, varchar, text, char, boolean, timestamp, date, time, jsonb, json, pgEnum, numeric, real, doublePrecision, bigint, serial, bigserial, pgPolicy } from 'drizzle-orm/pg-core';
import { relations as drizzleRelations, sql } from 'drizzle-orm';

export const ordersStatus = pgEnum("orders_status", ['pending', 'confirmed', 'processing', 'shipped', 'delivered', 'cancelled', 'refunded']);
export const ordersPayment_status = pgEnum("orders_payment_status", ['unpaid', 'paid', 'partially_refunded', 'refunded']);
export const ordersCurrency = pgEnum("orders_currency", ['USD', 'EUR', 'GBP', 'CAD', 'AUD']);
export const postsStatus = pgEnum("posts_status", ['draft', 'needs_review', 'published', 'archived']);
export const productsCategory = pgEnum("products_category", ['electronics', 'clothing', 'home_garden', 'sports', 'books', 'food_beverage', 'health_beauty', 'toys']);
export const productsStatus = pgEnum("products_status", ['draft', 'active', 'archived']);

export const authors = pgTable("authors", {
    id: integer("id").generatedByDefaultAsIdentity().primaryKey(),
    name: varchar("name").notNull(),
    email: varchar("email").notNull(),
    picture: varchar("picture"),
    userId: varchar("user_id")
}).enableRLS();

export const customers = pgTable("customers", {
    id: integer("id").generatedByDefaultAsIdentity().primaryKey(),
    first_name: varchar("first_name").notNull(),
    last_name: varchar("last_name").notNull(),
    email: varchar("email").unique().notNull(),
    phone: varchar("phone"),
    company: varchar("company"),
    shipping_address: varchar("shipping_address"),
    billing_address: varchar("billing_address"),
    notes: varchar("notes"),
    created_at: timestamp("created_at", { withTimezone: true, mode: 'string' }),
    updated_at: timestamp("updated_at", { withTimezone: true, mode: 'string' })
}, (table) => ([
    pgPolicy("test_policy", { as: "permissive", for: "all", to: ["public"], using: sql`true`, withCheck: sql`true` }),
])).enableRLS();

export const orderItems = pgTable("order_items", {
    id: integer("id").generatedByDefaultAsIdentity().primaryKey(),
    order_id: integer("order_id").references(() => orders.id, { onDelete: "set null" }),
    product_id: integer("product_id").references(() => products.id, { onDelete: "set null" }),
    product_name: varchar("product_name").notNull(),
    sku: varchar("sku"),
    quantity: numeric("quantity").notNull(),
    unit_price: numeric("unit_price").notNull(),
    line_total: numeric("line_total")
}, (table) => ([
    pgPolicy("test_policy", { as: "permissive", for: "all", to: ["public"], using: sql`true`, withCheck: sql`true` }),
])).enableRLS();

export const orders = pgTable("orders", {
    id: integer("id").generatedByDefaultAsIdentity().primaryKey(),
    order_number: varchar("order_number").unique().notNull(),
    customer_id: integer("customer_id").references(() => customers.id, { onDelete: "cascade" }).notNull(),
    status: ordersStatus("status").notNull(),
    payment_status: ordersPayment_status("payment_status").notNull(),
    subtotal: numeric("subtotal"),
    tax_amount: numeric("tax_amount"),
    shipping_cost: numeric("shipping_cost"),
    discount_amount: numeric("discount_amount"),
    total: numeric("total").notNull(),
    currency: ordersCurrency("currency"),
    shipping_address: varchar("shipping_address"),
    tracking_number: varchar("tracking_number"),
    notes: varchar("notes"),
    order_date: timestamp("order_date", { withTimezone: true, mode: 'string' }).notNull(),
    shipped_date: timestamp("shipped_date", { withTimezone: true, mode: 'string' }),
    delivered_date: timestamp("delivered_date", { withTimezone: true, mode: 'string' }),
    created_at: timestamp("created_at", { withTimezone: true, mode: 'string' }),
    updated_at: timestamp("updated_at", { withTimezone: true, mode: 'string' })
}, (table) => ([
    pgPolicy("test_policy", { as: "permissive", for: "all", to: ["public"], using: sql`true`, withCheck: sql`true` }),
])).enableRLS();

export const posts = pgTable("posts", {
    id: integer("id").generatedByDefaultAsIdentity().primaryKey(),
    title: varchar("title").notNull(),
    slug: varchar("slug").unique().notNull(),
    hero_image: varchar("hero_image"),
    excerpt: varchar("excerpt"),
    content: jsonb("content"),
    status: postsStatus("status").notNull(),
    publish_date: timestamp("publish_date", { withTimezone: true, mode: 'string' }),
    created_at: timestamp("created_at", { withTimezone: true, mode: 'string' }),
    updated_at: timestamp("updated_at", { withTimezone: true, mode: 'string' }),
    author_id: integer("author_id").references(() => authors.id, { onDelete: "set null" })
}, (table) => ([
    pgPolicy("test_policy", { as: "permissive", for: "all", to: ["public"], using: sql`true`, withCheck: sql`true` }),
])).enableRLS();

export const postsTags = pgTable("posts_tags", {
    post_id: integer("post_id").notNull().references(() => posts.id, { onDelete: "cascade" }),
    tag_id: integer("tag_id").notNull().references(() => tags.id, { onDelete: "cascade" }),
}, (table) => ({
    pk: primaryKey({ columns: [table.post_id, table.tag_id] })
}));

export const products = pgTable("products", {
    id: integer("id").generatedByDefaultAsIdentity().primaryKey(),
    name: varchar("name").notNull(),
    sku: varchar("sku").unique().notNull(),
    description: varchar("description"),
    image: varchar("image"),
    category: productsCategory("category").notNull(),
    price: numeric("price").notNull(),
    compare_at_price: numeric("compare_at_price"),
    cost: numeric("cost"),
    stock_quantity: numeric("stock_quantity").notNull(),
    low_stock_threshold: numeric("low_stock_threshold"),
    weight_grams: numeric("weight_grams"),
    status: productsStatus("status").notNull(),
    is_featured: boolean("is_featured"),
    created_at: timestamp("created_at", { withTimezone: true, mode: 'string' }),
    updated_at: timestamp("updated_at", { withTimezone: true, mode: 'string' })
}, (table) => ([
    pgPolicy("test_policy", { as: "permissive", for: "all", to: ["public"], using: sql`true`, withCheck: sql`true` }),
])).enableRLS();

export const profiles = pgTable("profiles", {
    id: integer("id").primaryKey().notNull(),
    bio: varchar("bio"),
    website: varchar("website"),
    author_id: integer("author_id").references(() => authors.id, { onDelete: "set null" })
}).enableRLS();

export const tags = pgTable("tags", {
    id: integer("id").primaryKey().notNull(),
    name: varchar("name").notNull()
}).enableRLS();

export const authorsRelations = drizzleRelations(authors, ({ one, many }) => ({
    "profile": one(profiles, {
        fields: [authors.id],
        references: [profiles.author_id],
        relationName: "profiles_author_id"
    }),
    "posts": many(posts, { relationName: "posts_author_id" })
}));

export const customersRelations = drizzleRelations(customers, ({ one, many }) => ({
    "orders": many(orders, { relationName: "orders_customer_id" })
}));

export const orderItemsRelations = drizzleRelations(orderItems, ({ one, many }) => ({
    "order": one(orders, {
        fields: [orderItems.order_id],
        references: [orders.id],
        relationName: "order_items_order_id"
    }),
    "product": one(products, {
        fields: [orderItems.product_id],
        references: [products.id],
        relationName: "order_items_product_id"
    })
}));

export const ordersRelations = drizzleRelations(orders, ({ one, many }) => ({
    "customer": one(customers, {
        fields: [orders.customer_id],
        references: [customers.id],
        relationName: "orders_customer_id"
    }),
    "order_items": many(orderItems, { relationName: "order_items_order_id" })
}));

export const postsRelations = drizzleRelations(posts, ({ one, many }) => ({
    "author": one(authors, {
        fields: [posts.author_id],
        references: [authors.id],
        relationName: "posts_author_id"
    }),
    "tags": many(postsTags, { relationName: "tags" })
}));

export const postsTagsRelations = drizzleRelations(postsTags, ({ one, many }) => ({
    "post_id": one(posts, {
        fields: [postsTags.post_id],
        references: [posts.id],
        relationName: "tags"
    }),
    "tag_id": one(tags, {
        fields: [postsTags.tag_id],
        references: [tags.id],
        relationName: "posts"
    })
}));

export const profilesRelations = drizzleRelations(profiles, ({ one, many }) => ({
    "author": one(authors, {
        fields: [profiles.author_id],
        references: [authors.id],
        relationName: "profiles_author_id"
    })
}));

export const tagsRelations = drizzleRelations(tags, ({ one, many }) => ({
    "posts": many(postsTags, { relationName: "posts" })
}));

export const tables = { authors, customers, orderItems, orders, posts, postsTags, products, profiles, tags };
export const enums = { ordersStatus, ordersPayment_status, ordersCurrency, postsStatus, productsCategory, productsStatus };
export const relations = { authorsRelations, customersRelations, orderItemsRelations, ordersRelations, postsRelations, postsTagsRelations, profilesRelations, tagsRelations };

