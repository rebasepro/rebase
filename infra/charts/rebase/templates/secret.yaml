{{- if not .Values.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "rebase.fullname" . }}-config
  labels:
    {{- include "rebase.labels" . | nindent 4 }}
type: Opaque
stringData:
  DATABASE_URL: {{ .Values.config.databaseUrl | quote }}
  JWT_SECRET: {{ .Values.config.jwtSecret | quote }}
  REBASE_SERVICE_KEY: {{ .Values.config.serviceKey | quote }}
  {{- if .Values.config.adminEmail }}
  REBASE_ADMIN_EMAIL: {{ .Values.config.adminEmail | quote }}
  REBASE_ADMIN_PASSWORD: {{ required "config.adminPassword is required when config.adminEmail is set (min 12 characters)." .Values.config.adminPassword | quote }}
  {{- end }}
  {{- if .Values.bundle.token }}
  REBASE_BUNDLE_TOKEN: {{ .Values.bundle.token | quote }}
  {{- end }}
{{- end }}
