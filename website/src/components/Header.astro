---
import NavigationDropdown from "./NavigationDropdown.astro";
import LanguageSelect from "./LanguageSelect.astro";
import { getLangFromUrl, useTranslations } from "../i18n/ui";

import {
    Layers, LayoutDashboard, Server, Bot, Code, Terminal, SquareTerminal, ShieldCheck, Rocket, ArrowRightLeft, Briefcase, FilePenLine
} from "lucide-react";

const currentPath = Astro.url.pathname.replace(/\/$/, "");
const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

const navLinks = [
    {
        href: "/features",
        label: t("nav.product"),
        dropdown: [
            [
                {
                    href: "/features",
                    label: t("nav.features"),
                    description: t("nav.features.desc"),
                    icon: Layers
                },
                {
                    href: "/studio",
                    label: t("nav.studio"),
                    description: t("nav.studio.desc"),
                    icon: LayoutDashboard
                },
                {
                    href: "/editing",
                    label: t("nav.editing"),
                    description: t("nav.editing.desc"),
                    icon: FilePenLine
                },
            ],
            [
                {
                    href: "/backend",
                    label: t("nav.backend"),
                    description: t("nav.backend.desc"),
                    icon: Server
                },
                {
                    href: "/ai",
                    label: t("nav.ai"),
                    description: t("nav.ai.desc"),
                    icon: Bot
                },
            ],
        ]
    },
    {
        href: "/developers",
        label: t("nav.developers"),
        dropdown: [
            [
                {
                    href: "/developers",
                    label: t("nav.devOverview"),
                    description: t("nav.devOverview.desc"),
                    icon: Code
                },
                {
                    href: "/sdk",
                    label: t("nav.sdk"),
                    description: t("nav.sdk.desc"),
                    icon: Terminal
                },
            ],
            [
                {
                    href: "/cli",
                    label: t("nav.cli"),
                    description: t("nav.cli.desc"),
                    icon: SquareTerminal
                },
                {
                    href: "/security",
                    label: t("nav.security"),
                    description: t("nav.security.desc"),
                    icon: ShieldCheck
                },
            ],
        ]
    },
    {
        href: "/why-rebase",
        label: t("nav.solutions"),
        dropdown: [
            [
                {
                    href: "/startups",
                    label: t("nav.startups"),
                    description: t("nav.startups.desc"),
                    icon: Rocket
                },
                {
                    href: "/why-rebase",
                    label: t("nav.whyRebase"),
                    description: t("nav.whyRebase.desc"),
                    icon: ArrowRightLeft
                },
            ],
            [
                {
                    href: "/agencies",
                    label: t("nav.agencies"),
                    description: t("nav.agencies.desc"),
                    icon: Briefcase
                },
            ],
        ]
    },
    {
        href: "/docs",
        label: t("nav.docs")
    },
];
---
<header class="bg-black/40 backdrop-blur sticky top-0 z-20">
    <nav class="mx-auto flex max-w-7xl items-center justify-between py-2 px-4 lg:px-8" aria-label="Global">
        <div class="flex items-center">
            <a href="/" class="flex items-center -m-1.5 p-1.5 relative z-50 mr-6" transition:name="site-logo">
                <img src="/img/rebase_logo.svg" class="h-8 w-8 mr-4" alt="Rebase Logo" transition:name="logo-image"/>
                <span class="self-center text-lg font-semibold whitespace-nowrap text-white"
                      transition:name="logo-text">Rebase</span>
            </a>
            <!-- Desktop Navigation -->
            <div class="hidden lg:flex items-center gap-2 text-sm font-semibold">
                {navLinks.map(link => (
                    link.dropdown ? (
                            <NavigationDropdown title={link.label} href={link.href}>
                                <div class="grid grid-cols-2 gap-x-8 gap-y-4 max-w-3xl">
                                    {link.dropdown.map(column => (
                                            <div class="flex flex-col gap-4 ">
                                                {column.map(subLink => {
                                                    const IconComp = subLink.icon;
                                                    return (
                                                        <a href={subLink.href}
                                                           class="flex items-start gap-4 p-3 rounded-lg hover:bg-surface-800 transition-colors focus:outline-none focus-visible:ring-2 focus-visible:ring-primary/60">
                                                            <IconComp class="text-primary w-6 h-6 flex-shrink-0" />
                                                            <div>
                                                                <p class="font-semibold text-white">{subLink.label}</p>
                                                                <p class="text-surface-400 text-sm">{subLink.description}</p>
                                                            </div>
                                                        </a>
                                                    );
                                                })}
                                            </div>
                                    ))}
                                </div>
                            </NavigationDropdown>
                    ) : (
                            <a
                                    href={link.href}
                                    class={`p-3 transition-colors hover:text-primary ${currentPath === link.href ? "text-primary" : "text-surface-300"}`}
                            >
                                {link.label}
                            </a>
                    )
                ))}
            </div>
        </div>
        <div class="flex flex-1 justify-end items-center gap-4">
            <LanguageSelect iconOnly={true} />
            <a
                    href="https://github.com/rebasepro/rebase"
                    target="_blank" rel="noopener noreferrer"
                    class="hidden sm:inline-flex items-center justify-center p-2 rounded-lg text-surface-400 hover:text-white hover:bg-white/10 transition-colors"
                    aria-label="GitHub"
                    data-track
                    id="header-github-link"
            >
                <svg class="h-5 w-5" viewBox="0 0 24 24" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
                    <path d="M12 2C6.477 2 2 6.484 2 12.017c0 4.425 2.865 8.18 6.839 9.504.5.092.682-.217.682-.483 0-.237-.008-.868-.013-1.703-2.782.605-3.369-1.343-3.369-1.343-.454-1.158-1.11-1.466-1.11-1.466-.908-.62.069-.608.069-.608 1.003.07 1.531 1.032 1.531 1.032.892 1.53 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.113-4.555-4.951 0-1.093.39-1.988 1.029-2.688-.103-.253-.446-1.272.098-2.65 0 0 .84-.27 2.75 1.026A9.564 9.564 0 0112 6.844c.85.004 1.705.115 2.504.337 1.909-1.296 2.747-1.027 2.747-1.027.546 1.379.202 2.398.1 2.651.64.7 1.028 1.595 1.028 2.688 0 3.848-2.339 4.695-4.566 4.943.359.309.678.92.678 1.855 0 1.338-.012 2.419-.012 2.747 0 .268.18.58.688.482A10.019 10.019 0 0022 12.017C22 6.484 17.522 2 12 2z"/>
                </svg>
            </a>
            <a
                    href="https://demo.rebase.pro"
                    target="_blank" rel="noopener noreferrer"
                    class="hidden sm:inline-flex items-center gap-x-2 rounded-lg bg-primary px-4 py-2 text-sm font-semibold text-white shadow-sm hover:bg-primary-dark focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary transition-all duration-200 ease-in-out btn-glow"
                    transition:name="start-building-button"
                    data-track
                    id="header-start-building-desktop"
            >
                {t("nav.tryDemo")}
<svg
  class="lucide lucide-arrow-right"
  xmlns="http://www.w3.org/2000/svg"
  width="18"
  height="18"
  viewBox="0 0 24 24"
  fill="none"
  stroke="currentColor"
  stroke-width="2"
  stroke-linecap="round"
  stroke-linejoin="round"
>
  <path d="M5 12h14" />
  <path d="m12 5 7 7-7 7" />
</svg>

            </a>
            <!-- Mobile menu button -->
            <button
                    type="button"
                    class="lg:hidden relative z-50 inline-flex items-center justify-center p-2 rounded-md text-surface-300 hover:text-white hover:bg-white/10 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-white transition-colors"
                    aria-controls="mobile-menu"
                    aria-expanded="false"
                    id="mobile-menu-button"
                    data-track
            >
                <span class="sr-only">Open main menu</span>
                <!-- Icon when menu is closed -->
                <svg class="block h-6 w-6" id="menu-icon-closed" viewBox="0 0 24 24" stroke-width="1.5"
                     stroke="currentColor" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round"
                          d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5"/>
                </svg>
                <!-- Icon when menu is open -->
                <svg class="hidden h-6 w-6" id="menu-icon-open" viewBox="0 0 24 24" stroke-width="1.5"
                     stroke="currentColor" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"/>
                </svg>
            </button>
        </div>
    </nav>

    <!-- Mobile menu - Full Screen Overlay -->
    <div class="lg:hidden hidden fixed inset-0 z-50 overflow-y-auto h-screen" id="mobile-menu">
        <div class="flex flex-col items-center justify-center min-h-screen px-6 py-20 bg-black/95 backdrop-blur-lg">
            <nav class="flex flex-col items-center space-y-8 w-full max-w-md">
                {navLinks.map((link, index) => (
                    link.dropdown ? (
                            <div class="w-full text-center">
                                <button
                                        type="button"
                                        id={`mobile-menu-toggle-${index}`}
                                        class="flex items-center justify-center text-2xl font-semibold transition-colors w-full text-center py-3 rounded-lg text-surface-300 hover:bg-white/10 hover:text-white"
                                        aria-expanded="false"
                                        aria-controls={`mobile-submenu-${index}`}
                                >
                                    {link.label}
                                    <svg class="w-5 h-5 ml-2 transition-transform" viewBox="0 0 20 20" fill="currentColor">
                                        <path d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z"/>
                                    </svg>
                                </button>
                                <div class="hidden flex-col items-center space-y-4 mt-4"
                                     id={`mobile-submenu-${index}`}>
                                    {link.dropdown.flat().map(subLink => (
                                            <a
                                                    href={subLink.href}
                                                    class={`block text-xl font-medium transition-colors w-full text-center py-2 rounded-lg ${
                                                        currentPath === subLink.href
                                                            ? "bg-primary/20 text-primary"
                                                            : "text-surface-400 hover:bg-white/10 hover:text-white"
                                                    }`}
                                            >
                                                {subLink.label}
                                            </a>
                                    ))}
                                </div>
                            </div>
                    ) : (
                            <a
                                    href={link.href}
                                    class={`block text-2xl font-semibold transition-colors w-full text-center py-3 rounded-lg ${
                                        currentPath === link.href
                                            ? "bg-primary/20 text-primary"
                                            : "text-surface-300 hover:bg-white/10 hover:text-white"
                                    }`}
                            >
                                {link.label}
                            </a>
                    )
                ))}
                <a
                        href="https://demo.rebase.pro"
                        target="_blank" rel="noopener noreferrer"
                        class="mt-8 w-full inline-flex items-center justify-center gap-x-2 rounded-lg bg-primary px-6 py-4 text-xl font-semibold text-white shadow-lg hover:bg-primary-dark transition-all duration-200 ease-in-out btn-glow"
                        data-track
                        id="header-start-building-mobile"
                >
                    {t("nav.tryDemo")}
                    
<svg
  class="lucide lucide-arrow-right"
  xmlns="http://www.w3.org/2000/svg"
  width="18"
  height="18"
  viewBox="0 0 24 24"
  fill="none"
  stroke="currentColor"
  stroke-width="2"
  stroke-linecap="round"
  stroke-linejoin="round"
>
  <path d="M5 12h14" />
  <path d="m12 5 7 7-7 7" />
</svg>

                </a>
                <div class="mt-4 w-full flex justify-center">
                    <LanguageSelect />
                </div>
            </nav>
        </div>
    </div>
</header>

<script is:inline>
    function setupHeader() {
        const mobileMenuButton = document.getElementById('mobile-menu-button');
        if (!mobileMenuButton || mobileMenuButton.dataset.initialized) {
            return;
        }
        mobileMenuButton.dataset.initialized = 'true';

        const mobileMenu = document.getElementById('mobile-menu');
        const menuIconClosed = document.getElementById('menu-icon-closed');
        const menuIconOpen = document.getElementById('menu-icon-open');

        mobileMenuButton.addEventListener('click', () => {
            const isExpanded = mobileMenuButton.getAttribute('aria-expanded') === 'true';

            if (isExpanded) {
                // Close menu
                mobileMenu.classList.add('hidden');
                menuIconClosed.classList.remove('hidden');
                menuIconOpen.classList.add('hidden');
                document.body.style.overflow = '';
                mobileMenuButton.setAttribute('aria-expanded', 'false');
            } else {
                // Open menu
                mobileMenu.classList.remove('hidden');
                menuIconClosed.classList.add('hidden');
                menuIconOpen.classList.remove('hidden');
                document.body.style.overflow = 'hidden';
                mobileMenuButton.setAttribute('aria-expanded', 'true');
            }
        });

        const mobileMenuToggles = document.querySelectorAll('[id^="mobile-menu-toggle-"]');
        mobileMenuToggles.forEach(toggle => {
            toggle.addEventListener('click', () => {
                const submenuId = toggle.getAttribute('aria-controls');
                const submenu = document.getElementById(submenuId);
                const isExpanded = toggle.getAttribute('aria-expanded') === 'true';

                toggle.setAttribute('aria-expanded', String(!isExpanded));
                submenu.classList.toggle('hidden');
                submenu.classList.toggle('flex');
                toggle.querySelector('svg')?.classList.toggle('rotate-180');
            });
        });

        // Track all header link clicks
        function trackHeaderLinkClick(link, linkType, isMobile = false) {
            if (!link.dataset.trackingAdded) {
                link.dataset.trackingAdded = 'true';
                link.addEventListener('click', function(e) {
                    const href = link.getAttribute('href');
                    const linkText = link.textContent?.trim() || '';
                    // Send analytics event
                    if (typeof window.gtag === 'function') {
                        const eventName = 'header_link_click';
                        window.gtag('event', eventName, {
                            event_category: 'header_navigation',
                            event_label: href,
                            link_type: linkType,
                            link_text: linkText,
                            link_url: href,
                            is_mobile: isMobile,
                            page_path: window.location.pathname
                        });
                    }

                    // Log to console for debugging
                    console.log(`Header link clicked: ${linkText} (${href}) - ${linkType}${isMobile ? ' [mobile]' : ''}`);
                });
            }
        }

        // Track desktop navigation links
        const desktopLinks = document.querySelectorAll('header nav > div.hidden.lg\\:flex a');
        desktopLinks.forEach(link => trackHeaderLinkClick(link, 'desktop_nav', false));

        // Track desktop CTA button
        const desktopCTA = document.querySelector('header nav a[href*="/docs"].hidden.sm\\:inline-flex');
        if (desktopCTA) trackHeaderLinkClick(desktopCTA, 'desktop_cta', false);

        // Track logo link
        const logoLink = document.querySelector('header a[href="/"]');
        if (logoLink) trackHeaderLinkClick(logoLink, 'logo', false);

        // Track mobile menu links
        const mobileLinks = document.querySelectorAll('#mobile-menu a');
        mobileLinks.forEach(link => trackHeaderLinkClick(link, 'mobile_nav', true));
    }

    document.addEventListener('astro:page-load', setupHeader);
</script>
