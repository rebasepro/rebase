---
sourceHash: 08a0e2fca6391df2
title: Einführung
sidebar_label: Einführung
description: Rebase ist ein quelloffenes Backend-as-a-Service und Admin-Panel, das mit React und TypeScript erstellt wurde. Definieren Sie Ihr Schema in TypeScript, und Rebase generiert eine vollständige CRUD-Benutzeroberfläche, eine REST-API, Authentifizierung, Speicher und Echtzeit-Synchronisierung – betrieben von PostgreSQL.
---

import { AdminDemoCarousel } from "../../../../components/demos/AdminDemoCarousel";

<AdminDemoCarousel client:only="react" height={400} showTabs={false} defaultTab={1} autoPlay={false} scale={0.75} />

## Was ist Rebase?

Rebase ist ein **quelloffenes Backend-as-a-Service (BaaS)** und **Admin-Panel**, das mit React und TypeScript erstellt wurde. Es bietet Ihnen alles, was Sie zum Erstellen produktionsbereiter Back-Office-Anwendungen auf Basis von **PostgreSQL** benötigen:

- **Schema als Code** — Definieren Sie Ihre Datenmodelle als TypeScript-Sammlungen. Rebase generiert PostgreSQL-Tabellen, eine vollständige CRUD-Benutzeroberfläche, REST-API-Endpunkte und typsichere Client-SDKs aus einer einzigen Quelle der Wahrheit.
- **Sofortiges Admin-Panel** — Eine schöne, schnelle React-basierte Admin-Benutzeroberfläche mit Tabellen, Listen, Formularen, Kanban-Boards, Inline-Bearbeitung und Echtzeit-Synchronisierung.
- **Integriertes Backend** — Authentifizierung (Passwörter, Magic Links, Einmalcodes, MFA, API-Schlüssel und zwölf OAuth-/OIDC-Provider), Dateispeicherung (lokal oder S3), Row Level Security, Entitätshistorie und WebSocket-Echtzeit – alles integriert.
- **Radikal erweiterbar** — Wenn Sie es in React erstellen können, können Sie es zu Rebase hinzufügen. Benutzerdefinierte Felder, benutzerdefinierte Ansichten, Plugins und voller Zugriff auf interne Hooks.

:::tip
Rebase läuft auf beide Arten, und Ihr Code ändert sich dabei nicht. **Hosten Sie es selbst** — Ihnen gehören die Daten, der Code und die Maschine, auf der es läuft. Oder deployen Sie dasselbe Projekt in die **Rebase Cloud** ([app.rebase.pro](https://app.rebase.pro)): dasselbe Rebase, von uns betrieben, heute mit echten Tenants live und während der Private Beta schrittweise geöffnet. Betrachten Sie es als eine quelloffene Alternative zu Supabase oder Retool, bei der *Sie* entscheiden, wer sie betreibt.
:::

## So funktioniert es

```
┌──────────────────────────────────────────────────────┐
│                 Client Applications                  │
│  ┌──────────────┐  ┌───────────────────────────────┐ │
│  │   Your App   │  │   Rebase Admin UI             │ │
│  │  (Any Tech)  │  │   Tables / Lists / Kanban     │ │
│  └──────┬───────┘  └───────┬───────────────────────┘ │
└─────────┼───────────────────┼───────────────────────┘
          │  HTTP + WebSocket   │
          ▼                     ▼
┌──────────────────────────────────────────────────────┐
│              Rebase Backend (Node.js + Hono)         │
│  ┌─────────┐ ┌──────┐ ┌─────────┐ ┌───────────────┐  │
│  │ REST API│ │ Auth │ │ Storage │ │ WebSocket RT  │  │
│  └────┬────┘ └──┬───┘ └────┬────┘ └──────┬────────┘  │
└───────┼─────────┼──────────┼─────────────┼───────────┘
        │         │          │             │
        ▼         ▼          ▼             ▼
┌──────────────────────────────────────────────────────┐
│              PostgreSQL (Drizzle ORM)                │
└──────────────────────────────────────────────────────┘
```

## Schnellstart

```bash
pnpm dlx @rebasepro/cli init my-app
cd my-app
```

`init` erzeugt eine startklare `.env` für Sie – nichts muss bearbeitet werden. Dann:

```bash
pnpm install
pnpm run dev
```

Das ist der gesamte erste Start. Es gibt keine Datenbank zu installieren und keinen Schema-Schritt: ohne gesetzte `DATABASE_URL` startet `rebase dev` eine verwaltete PostgreSQL (PGlite) im Projektverzeichnis, erzeugt das Schema aus Ihren Collections und legt die Tabellen beim Start an.

**Lesen Sie die URLs aus der Ausgabe.** `rebase dev` wählt pro Projekt einen freien Port statt fester Ports, sodass sie sich zwischen Projekten und Maschinen unterscheiden. Das erste Konto, das Sie registrieren, wird zum Administrator.

Um stattdessen Ihre eigene PostgreSQL zu verwenden, kommentieren Sie `DATABASE_URL` in `.env` ein und führen Sie `pnpm run dev` erneut aus.

→ Folgen Sie dem vollständigen [Schnellstart-Leitfaden](/docs/getting-started/quickstart) für eine komplette Anleitung, einschließlich der Anbindung Ihrer eigenen Datenbank.

## Kernfunktionen

| Funktion | Beschreibung |
|---------|-------------|
| **Tabellenansichten** | Schnelle, virtualisierte Tabelle mit Inline-Bearbeitung, Filterung, Sortierung und Textsuche |
| **Listenansicht** | Saubere, responsive vertikale Listenansicht für Sammlungen |
| **Kanban-Board** | Drag-and-Drop-Board-Ansicht, gruppiert nach einer beliebigen Enum-Eigenschaft |
| **Beziehungen** | Eins-zu-eins, Eins-zu-viele, Viele-zu-viele mit Verknüpfungstabellen und Multi-Hop-Joins |
| **Zeilenbasierte Sicherheit (RLS)** | RLS-Richtlinien im Supabase-Stil, definiert in Ihrer Sammlungs-Konfiguration |
| **Authentifizierung** | JWT + Refresh-Token, zwölf OAuth-/OIDC-Provider, Magic Links, Einmalcodes, TOTP-MFA, API-Schlüssel mit Geltungsbereich, rollenbasierte Zugriffssteuerung |
| **Dateispeicherung** | Lokales Dateisystem oder S3-kompatibel mit Upload-Feldern und Browser |
| **Echtzeit-Synchronisierung** | WebSocket-gesteuerte Live-Updates für alle verbundenen Clients |
| **Entitätshistorie** | Vollständige Überwachungshistorie für jede Erstellung, Aktualisierung und Löschung |
| **REST-API** | Automatisch generierte CRUD-Endpunkte für jede Sammlung |
| **Datenimport/-export** | CSV-, JSON- und Excel-Import mit Feldzuordnung; CSV-/JSON-Export |
| **Sammlungs-Editor** | Visueller Schema-Editor, der TypeScript über AST-Manipulation generiert |
| **Client-SDK** | Typsicheres JavaScript-SDK für Ihre Frontend-Anwendungen |
| **Plugin-System** | Erweitern Sie jeden Teil der Benutzeroberfläche — Symbolleiste, Formulare, Felder, Startseite |
| **KI-Datenverbesserung** | LLM-gestützte Autovervollständigung für Textfelder |
| **Cron-Jobs** | Geplante Hintergrundaufgaben mit Überwachung, Protokollierung und Studio-Dashboard |

## Nächste Schritte

- **[Schnellstart](/docs/getting-started/quickstart)** — In 2 Minuten einsatzbereit
- **[Projektstruktur](/docs/getting-started/project-structure)** — Den generierten Code verstehen
- **[Sammlungen](/docs/collections)** — Ihr Datenschema definieren
- **[Backend](/docs/backend)** — Authentifizierung, Speicher und die API konfigurieren
- **[Architektur](/docs/architecture)** — Verstehen, wie alles zusammenpasst
---
