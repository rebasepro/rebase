---
title: Introdução
sidebar_label: Introdução
description: Rebase é um Backend-as-a-Service e painel de administração de código aberto, construído com React e TypeScript. Defina seu esquema em TypeScript, e o Rebase gera uma UI CRUD completa, API REST, autenticação, armazenamento e sincronização em tempo real — tudo alimentado por PostgreSQL.
---

import { AdminDemoCarousel } from "../../../../components/demos/AdminDemoCarousel";

<AdminDemoCarousel client:only="react" height={400} showTabs={false} defaultTab={1} autoPlay={false} scale={0.75} />

## O que é Rebase?

Rebase é um **Backend-as-a-Service (BaaS) de código aberto** e **painel de administração** construído com React e TypeScript. Ele oferece tudo o que você precisa para construir aplicações back-office prontas para produção sobre **PostgreSQL**:

- **Schema as Code** — Defina seus modelos de dados como coleções TypeScript. O Rebase gera tabelas PostgreSQL, uma UI CRUD completa, endpoints de API REST e SDKs cliente com tipos seguros a partir de uma única fonte de verdade.
- **Instant Admin Panel** — Uma UI de administração bonita e rápida baseada em React, com tabelas de planilha, listas, formulários, quadros Kanban, edição inline e sincronização em tempo real.
- **Integrated Backend** — Autenticação (JWT + Google OAuth), armazenamento de arquivos (local ou S3), Segurança em Nível de Linha (RLS), histórico de entidades e WebSocket em tempo real — tudo integrado.
- **Radically Extensible** — Se você pode construir em React, pode adicionar ao Rebase. Campos personalizados, visualizações personalizadas, plugins e acesso total a hooks internos.

:::tip
O Rebase funciona das duas formas, e o seu código não muda entre elas. **Auto-hospede-o** — é o proprietário dos dados, do código e da máquina onde corre. Ou faça o deploy do mesmo projeto na **Rebase Cloud** ([app.rebase.pro](https://app.rebase.pro)): o mesmo Rebase, operado por nós, hoje em produção com tenants reais e a abrir por lotes durante a beta privada. Pense nele como uma alternativa de código aberto ao Supabase ou Retool em que é *você* quem decide quem a opera.
:::

## Como Funciona

```
┌──────────────────────────────────────────────────────┐
│                 Client Applications                  │
│  ┌──────────────┐  ┌───────────────────────────────┐ │
│  │   Your App   │  │   Rebase Admin UI             │ │
│  │  (Any Tech)  │  │   Tables / Lists / Kanban     │ │
│  └──────┬───────┘  └──────────────┬────────────────┘ │
└─────────┼──────────────────────────┼─────────────────┘
          │  HTTP + WebSocket        │
          ▼                          ▼
┌──────────────────────────────────────────────────────┐
│              Rebase Backend (Node.js + Hono)         │
│  ┌─────────┐ ┌──────┐ ┌─────────┐ ┌───────────────┐  │
│  │ REST API│ │ Auth │ │ Storage │ │ WebSocket RT  │  │
│  └────┬────┘ └──┬───┘ └────┬────┘ └──────┬────────┘  │
└───────┼─────────┼──────────┼─────────────┼───────────┘
        │         │          │             │
        ▼         ▼          ▼             ▼
┌──────────────────────────────────────────────────────┐
│              PostgreSQL (Drizzle ORM)                │
└──────────────────────────────────────────────────────┘
```

## Início Rápido

```bash
pnpm dlx @rebasepro/cli init my-app
cd my-app
```

O `init` gera um arquivo `.env` pronto para uso (segredos, senha do banco de dados, uma porta local livre) e se oferece para instalar as dependências — sem necessidade de edição para começar. Então:

```bash
pnpm dev                  # banco de dados, esquema, backend + frontend
```

Essa é a primeira execução inteira. O `rebase dev` inicia um PostgreSQL gerenciado para o projeto, gera o esquema do banco de dados a partir das suas coleções, aplica-o e serve — sem Docker e sem mais nada para instalar.

Ele imprime as duas URLs às quais se vinculou. Essas portas são derivadas do caminho do projeto para que vários projetos Rebase possam rodar ao mesmo tempo, então use as URLs do seu terminal em vez de `localhost:5173`. A primeira conta que você registrar torna-se a de administrador.

→ Siga o [guia de Início Rápido](/docs/getting-started/quickstart) completo para um passo a passo detalhado, incluindo como conectar seu próprio banco de dados.

## Funcionalidades Principais

| Funcionalidade | Descrição |
|---------|-------------|
| **Spreadsheet Views** | Tabela rápida e virtualizada com edição inline, filtragem, ordenação e pesquisa de texto |
| **List View** | Visualização de lista vertical limpa e responsiva para coleções |
| **Kanban Board** | Visualização em quadro de arrastar e soltar agrupada por qualquer propriedade enum |
| **Relations** | Um-para-um, um-para-muitos, muitos-para-muitos com tabelas de junção e junções de múltiplos saltos |
| **Row Level Security** | Políticas RLS estilo Supabase definidas na configuração da sua coleção |
| **Authentication** | JWT + tokens de atualização, Google OAuth, controle de acesso baseado em função |
| **File Storage** | Sistema de arquivos local ou compatível com S3, com campos de upload e navegador |
| **Real-time Sync** | Atualizações ao vivo via WebSocket em todos os clientes conectados |
| **Entity History** | Trilha de auditoria completa para cada criação, atualização e exclusão |
| **REST API** | Endpoints CRUD auto-gerados para cada coleção |
| **Data Import/Export** | Importação CSV, JSON e Excel com mapeamento de campos; exportação CSV/JSON |
| **Collection Editor** | Editor de esquema visual que gera TypeScript via manipulação AST |
| **Client SDK** | SDK JavaScript com tipo seguro para suas aplicações frontend |
| **Plugin System** | Estenda cada parte da UI — barra de ferramentas, formulários, campos, página inicial |
| **AI Data Enhancement** | Preenchimento automático alimentado por LLM para campos de texto |
| **Cron Jobs** | Tarefas em segundo plano agendadas com monitoramento, registro e painel do Studio |

## Próximos Passos

- **[Início Rápido](/docs/getting-started/quickstart)** — Comece a usar em 2 minutos
- **[Estrutura do Projeto](/docs/getting-started/project-structure)** — Entenda o código gerado
- **[Coleções](/docs/collections)** — Defina seu esquema de dados
- **[Backend](/docs/backend)** — Configure autenticação, armazenamento e a API
- **[Arquitetura](/docs/architecture)** — Entenda como tudo se encaixa

---
