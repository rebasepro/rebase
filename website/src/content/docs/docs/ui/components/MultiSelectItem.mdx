---
title: MultiSelectItem
sidebar_label: MultiSelectItem
description: MultiSelectItem — a component from @rebasepro/ui, the library the Rebase admin panel is built from.
---

```ts
import { MultiSelectItem } from "@rebasepro/ui";
```

## Props

| Prop | Type | Required | Description |
| --- | --- | --- | --- |
| `value` | `T` | yes |  |
| `children` | `React.ReactNode` | — |  |
| `className` | `string` | — |  |

## Example

This is the example the design-system sync renders and verifies for this component.

```tsx
import { MultiSelect, MultiSelectItem } from "@rebasepro/ui";

export const InMultiSelect = () => (
    <div className="w-[300px] p-4">
        <MultiSelect label="Roles" useChips value={["admin", "viewer"]} onValueChange={() => {}}>
            <MultiSelectItem value="admin">Admin</MultiSelectItem>
            <MultiSelectItem value="editor">Editor</MultiSelectItem>
            <MultiSelectItem value="viewer">Viewer</MultiSelectItem>
        </MultiSelect>
    </div>
);
```
