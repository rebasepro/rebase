/**
 * Off-site destinations the whole site shares.
 *
 * The demo used to be a page here (`/demo`) that framed an embedded widget and
 * linked on to the real thing. Two clicks to reach one panel, so the page is
 * gone: every "Try the demo" now goes straight to the hosted backend. Keeping
 * the URL in one place is what stops the next CTA from inventing its own.
 */
export const DEMO_URL = "https://demo.rebase.pro";
