---

import { languages, defaultLang, useTranslations } from '../../i18n/ui';

export function getStaticPaths() {
  return Object.keys(languages).map((lang) => ({
    params: { lang: lang === defaultLang ? undefined : lang },
  }));
}

const { lang } = Astro.params;
const currentLang = lang || defaultLang;
const t = useTranslations(currentLang as keyof typeof languages);

import Layout from '../../layouts/Layout.astro';

const title = t("rebase-vs-supabase.meta.title");
const description = t("rebase-vs-supabase.meta.description");

import RebaseVsSupabaseContent from '../../components/pages/RebaseVsSupabaseContent.astro';
const breadcrumbs = [
    { name: "Home", url: "https://rebase.pro" },
    { name: "Rebase vs Supabase", url: "https://rebase.pro/rebase-vs-supabase" }
];
---
<Layout title={title} description={description} breadcrumbs={breadcrumbs}>
    <RebaseVsSupabaseContent lang={currentLang} t={t} />
</Layout>
