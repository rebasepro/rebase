---

import { languages, defaultLang, useTranslations } from '../../i18n/ui';

export function getStaticPaths() {
  return Object.keys(languages).map((lang) => ({
    params: { lang: lang === defaultLang ? undefined : lang },
  }));
}

const { lang } = Astro.params;
const currentLang = lang || defaultLang;
const t = useTranslations(currentLang as keyof typeof languages);

import Layout from '../../layouts/Layout.astro';

const title = t("rebase-vs-strapi.meta.title");
const description = t("rebase-vs-strapi.meta.description");

import RebaseVsStrapiContent from '../../components/pages/RebaseVsStrapiContent.astro';
const breadcrumbs = [
    { name: "Home", url: "https://rebase.pro" },
    { name: "Rebase vs Strapi", url: "https://rebase.pro/rebase-vs-strapi" }
];
---
<Layout title={title} description={description} breadcrumbs={breadcrumbs}>
    <RebaseVsStrapiContent lang={currentLang} t={t} />
</Layout>
