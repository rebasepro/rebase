---

import { languages, defaultLang, useTranslations } from '../../i18n/ui';

export function getStaticPaths() {
  return Object.keys(languages).map((lang) => ({
    params: { lang: lang === defaultLang ? undefined : lang },
  }));
}

const { lang } = Astro.params;
const currentLang = lang || defaultLang;
const t = useTranslations(currentLang as keyof typeof languages);

import Layout from '../../layouts/Layout.astro';

const title = t("rebase-vs-directus.meta.title");
const description = t("rebase-vs-directus.meta.description");

import RebaseVsDirectusContent from '../../components/pages/RebaseVsDirectusContent.astro';
const breadcrumbs = [
    { name: "Home", url: "https://rebase.pro" },
    { name: "Rebase vs Directus", url: "https://rebase.pro/rebase-vs-directus" }
];
---
<Layout title={title} description={description} breadcrumbs={breadcrumbs}>
    <RebaseVsDirectusContent lang={currentLang} t={t} />
</Layout>
