---

import { languages, defaultLang, useTranslations } from '../../i18n/ui';

export function getStaticPaths() {
  return Object.keys(languages).map((lang) => ({
    params: { lang: lang === defaultLang ? undefined : lang },
  }));
}

const { lang } = Astro.params;
const currentLang = lang || defaultLang;
const t = useTranslations(currentLang as keyof typeof languages);

import Layout from '../../layouts/Layout.astro';

const title = t("rebase-vs-firebase.meta.title");
const description = t("rebase-vs-firebase.meta.description");

import RebaseVsFirebaseContent from '../../components/pages/RebaseVsFirebaseContent.astro';
const breadcrumbs = [
    { name: "Home", url: "https://rebase.pro" },
    { name: "Rebase vs Firebase", url: "https://rebase.pro/rebase-vs-firebase" }
];
---
<Layout title={title} description={description} breadcrumbs={breadcrumbs}>
    <RebaseVsFirebaseContent lang={currentLang} t={t} />
</Layout>
