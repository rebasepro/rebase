---

import { languages, defaultLang, useTranslations } from '../../i18n/ui';

export function getStaticPaths() {
  return Object.keys(languages).map((lang) => ({
    params: { lang: lang === defaultLang ? undefined : lang },
  }));
}

const { lang } = Astro.params;
const currentLang = lang || defaultLang;
const t = useTranslations(currentLang as keyof typeof languages);

import Layout from '../../layouts/Layout.astro';

const title = t("rebase-vs-payload.meta.title");
const description = t("rebase-vs-payload.meta.description");

import RebaseVsPayloadContent from '../../components/pages/RebaseVsPayloadContent.astro';
const breadcrumbs = [
    { name: "Home", url: "https://rebase.pro" },
    { name: "Rebase vs Payload", url: "https://rebase.pro/rebase-vs-payload" }
];
---
<Layout title={title} description={description} breadcrumbs={breadcrumbs}>
    <RebaseVsPayloadContent lang={currentLang} t={t} />
</Layout>
