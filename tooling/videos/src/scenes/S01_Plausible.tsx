import React from "react";
import { Scene } from "../components/Scene";
import { Plausible } from "../reel/Plausible";

/**
 * 01 · THE PROBLEM — 300 frames. The film's hook.
 *
 * It replaces an opening on the second copy, which is SITE-STORY's claim TWO
 * and the less differentiating half: everything from Prisma to Payload attacks
 * duplication, and a viewer using any of them hears "another codegen tool".
 * Claim one is security, rls-check is the acquisition wedge, and nobody else in
 * this category can make that claim — so it is what the film opens on.
 */
export const S01_Plausible: React.FC = () => (
    <Scene>
        <Plausible />
    </Scene>
);
