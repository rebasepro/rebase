{
    "access": "per-field `access` rules shipped before 0.22; entries fix doors that skipped them",
    "allowedRedirectUris": "the OAuth option shipped before 0.22; the entry applies it to the link route",
    "authController": "the `<Rebase authController>` prop shipped long ago; the entry fixes the fallback when it is absent",
    "beforeUserCreate": "the auth hook shipped long ago; the entry runs it on /auth/anonymous/link too",
    "clearOnDisabled": "the property option shipped long ago; the entry fixes restoring a falsy value",
    "create_document": "the MCP tool shipped with the MCP server; the entry applies field write rules to it",
    "email": "the `email` field/validation and email sign-in shipped long ago; entries fix bugs in them",
    "filterValues": "`useCollection`'s `filterValues` shipped long ago; the entry fixes inline objects re-subscribing",
    "Idempotency-Key": "the Idempotency-Key header shipped before 0.22; entries fix nested routes and the fingerprint",
    "If-Match": "conditional writes with If-Match shipped before 0.22; entries fix them",
    "Infinity": "not a feature name — the entry says `timeoutSeconds: Infinity` now means no timeout",
    "intervalMs": "the RLS audit's `intervalMs` shipped before 0.22; the entry adds range checking",
    "length": "string `validation.length` shipped long ago; the entry makes the form check it",
    "lessThan": "number `validation.lessThan` shipped long ago; the entry fixes its message",
    "listenById()": "the SDK's `listenById()` shipped long ago; the entry fixes it for anonymous visitors",
    "lowercase": "`validation.lowercase` shipped long ago; the entry makes it change the saved value",
    "maxResults": "storage listing's `maxResults` shipped long ago; the entry adds validation",
    "meta.total": "`meta.total` on list responses shipped long ago; the entry corrects the OpenAPI text for distinct reads",
    "MFA_REQUIRED": "the server has answered `MFA_REQUIRED` since MFA shipped; the entry adds the admin's second step",
    "moreThan": "number `validation.moreThan` shipped long ago; the entry fixes its message",
    "nextCursor": "keyset `nextCursor` pagination shipped before 0.22; entries fix cursors over µs timestamps, composite keys and vector search",
    "numeric": "`numeric` columns with precision/scale shipped long ago; the entry fixes the drift report",
    "on_update": "`autoValue: \"on_update\"` shipped long ago; the entry fixes If-Match on such collections",
    "pageToken": "storage listing's `pageToken` shipped long ago; the entry adds validation",
    "policy-always-true": "an rls-check check id since rls-check's first release; the entry fixes its restrictive-policy downgrade",
    "read": "the `read` operation of API-key permissions and `access.read` field rules shipped long ago; entries fix how they are applied",
    "rebase db push": "the command has shipped since 0.1; the entries fix its destructive gate and scratch database",
    "rebase_auth_reset_password": "the MCP tool shipped with the MCP server; the entry fixes its user lookup",
    "REBASE_RATE_LIMIT_STORE": "the variable shipped before 0.22; the entry makes the auth limiters honour it",
    "register": "the `register` captcha route shipped long ago; the entry extends it to /auth/anonymous/link",
    "time": "`columnType: \"time\"` shipped long ago; the entry fixes its date default",
    "timeoutSeconds": "a cron job's `timeoutSeconds` shipped before 0.22; the entry fixes values past the timer ceiling",
    "trim": "`validation.trim` shipped long ago; the entry makes it change the saved value",
    "update_document": "the MCP tool shipped with the MCP server; the entry applies field write rules to it",
    "uppercase": "`validation.uppercase` shipped long ago; the entry makes it change the saved value",
    "useCollection": "the hook shipped long ago; the entry fixes a re-subscribe loop",
    "using": "a policy's USING clause in security rules shipped long ago; entries fix its import",
    "withCheck": "raw `withCheck` security rules shipped long ago; entries fix how an INSERT policy's check is kept"
}
